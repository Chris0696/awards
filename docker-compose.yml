services:
  postgres_db:
    image: postgres:16.1-bookworm
    ports:
      - "5434:5432"
    volumes:
      - awards_local_pgdata:/var/lib/postgresql/data
    env_file: .env
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASS}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-postgres} -d $${POSTGRES_DB:-postgres}"]
      interval: 3s
      timeout: 5s
      retries: 5
      start_period: 10s
    logging:
      driver: json-file
      options:
        max-file: "3"
        max-size: 15m
    networks:
      - awards-bridge

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      # 8001 sur l'hôte pour éviter conflit avec un autre projet sur 8000
      - "8001:8000"
    depends_on:
      postgres_db:
        condition: service_healthy
    restart: on-failure
    volumes:
      - ./backend:/usr/src/app
      - awards_collectstatic:/usr/src/app/collectstatic
      - ./backend/log:/usr/src/app/log
      - ./backend/media:/usr/src/app/media
      - ./.env:/usr/src/app/.env:ro
    env_file: .env
    command: sh -c "chmod +x wait_for_db.sh && ./wait_for_db.sh && python manage.py runserver 0.0.0.0:8000"
    networks:
      - awards-bridge

  web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules

    env_file:
      - .env
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:8000
      # URL du backend pour le navigateur (depuis l’hôte = localhost:8001)
      - NEXT_PUBLIC_BACKEND_URL=http://localhost:8001/api/v1/
    networks: # Ajouté le réseau manquant
      - awards-bridge

networks:
  awards-bridge:
    driver: bridge

volumes:
  awards_local_pgdata: {}
  awards_collectstatic: {}
